# Frontend build-time configuration.
# During development the Vite dev server proxies /api and /socket.io to the
# backend (see vite.config.ts), so no variables are strictly required.
# In production the app is served behind NGINX which proxies the same paths.

# Optional: override the API base if the API is on a different origin.
# VITE_API_BASE_URL=/api
